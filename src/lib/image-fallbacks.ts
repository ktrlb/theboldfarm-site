/**
 * Image fallback constants
 * Centralized fallback image paths for consistent use across the site
 */

// Default fallback image for hero sections and feature cards
export const DEFAULT_FALLBACK_IMAGE = '/bold farm pond.png';

